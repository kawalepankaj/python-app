apiVersion: v1
kind: Service
metadata:
  name: sample-fastapi-app
  labels:
    app.kubernetes.io/name: sample-fastapi-app
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: sample-fastapi-app
  ports:
    - name: http
      port: 80
      targetPort: http
